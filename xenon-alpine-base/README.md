Base Docker image based off of Linux Alpine

Build with:

```bash
docker build --tag nlesc/xenon-alpine-base .
```

Run with:
```bash
docker run --user xenon --rm --interactive --tty nlesc/xenon-alpine-base bash
```

